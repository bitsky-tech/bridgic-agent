diff --git a/lib/cjs/index.js b/lib/cjs/index.js
index aa6473bee22fcae6bbdaaad2c8d5cb4893ed4b11..cdf00ac974cacce3075df5bfd32785961e5af522 100644
--- a/lib/cjs/index.js
+++ b/lib/cjs/index.js
@@ -4384,6 +4384,7 @@ function ContextMenuMenu(props) {
 	const { menuSchemas, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds, onOptionSelect, maxMenuHeight } = props;
 	const localeService = (0, _wendellhu_redi_react_bindings.useDependency)(_univerjs_core.LocaleService);
 	const hiddenGroupStates = useContextGroupHiddenStates$1(menuSchemas);
+	const [activeMenuItem, setActiveMenuItem] = (0, react.useState)(null);
 	const visibleSchemas = (0, react.useMemo)(() => {
 		return menuSchemas.filter((item) => {
 			if (!hasRenderableContextMenuSchema(item)) return false;
@@ -4399,6 +4400,8 @@ function ContextMenuMenu(props) {
 			menuItem: menuSchema.item,
 			menuSessionVersion,
 			submenuPortalContainer,
+			activeMenuItem,
+			onMenuItemEnter: setActiveMenuItem,
 			onOptionSelect,
 			maxMenuHeight,
 			hiddenItemIds
@@ -4406,6 +4409,7 @@ function ContextMenuMenu(props) {
 		if (!((_menuSchema$children2 = menuSchema.children) === null || _menuSchema$children2 === void 0 ? void 0 : _menuSchema$children2.length)) return null;
 		if (menuSchema.quickLayout) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
 			className: (0, _univerjs_design.clsx)("univer-py-1", hasSeparator && _univerjs_design.borderBottomClassName),
+			onMouseEnter: () => setActiveMenuItem(null),
 			children: menuSchema.quickLayout === "tile" ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(UIQuickTileMenuGroup, {
 				item: menuSchema,
 				activeItemIds,
@@ -4425,6 +4429,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4442,6 +4448,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4451,7 +4459,7 @@ function ContextMenuMenu(props) {
 	}) });
 }
 function ContextMenuMenuItem(props) {
-	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
+	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeMenuItem, onMenuItemEnter, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
 	const localeService = (0, _wendellhu_redi_react_bindings.useDependency)(_univerjs_core.LocaleService);
 	const direction = (0, _wendellhu_redi_react_bindings.useObservable)(localeService.direction$);
 	const menuManagerService = (0, _wendellhu_redi_react_bindings.useDependency)(IMenuManagerService);
@@ -4463,6 +4471,7 @@ function ContextMenuMenuItem(props) {
 	const selectionsFromObservable = (0, _wendellhu_redi_react_bindings.useObservable)((0, rxjs.isObservable)(selectorItem.selections) ? selectorItem.selections : void 0);
 	const [inputValue, setInputValue] = (0, react.useState)(value);
 	const [submenuVisible, setSubmenuVisible] = (0, react.useState)(false);
+	const activeSubmenuVisible = submenuVisible && activeMenuItem === menuItem;
 	const [submenuPosition, setSubmenuPosition] = (0, react.useState)({
 		left: 0,
 		top: 0
@@ -4511,7 +4520,7 @@ function ContextMenuMenuItem(props) {
 	}, [value]);
 	(0, react.useEffect)(() => () => clearSubmenuCloseTimer(), [clearSubmenuCloseTimer]);
 	(0, react.useEffect)(() => {
-		if (!submenuVisible) {
+		if (!activeSubmenuVisible) {
 			setSubmenuPositionReady(false);
 			return;
 		}
@@ -4542,7 +4551,7 @@ function ContextMenuMenuItem(props) {
 			window.removeEventListener("scroll", updateSubmenuPosition, true);
 		};
 	}, [
-		submenuVisible,
+		activeSubmenuVisible,
 		hasSelectionSubmenu,
 		hasSubItemSubmenu
 	]);
@@ -4593,6 +4602,7 @@ function ContextMenuMenuItem(props) {
 		ref: menuItemElementRef,
 		className: "univer-relative",
 		onMouseEnter: () => {
+			onMenuItemEnter(menuItem);
 			clearSubmenuCloseTimer();
 			if (hasSubmenu && !disabled) {
 				setSubmenuPositionReady(false);
@@ -4617,6 +4627,7 @@ function ContextMenuMenuItem(props) {
 			disabled,
 			title: compact && typeof menuItem.tooltip === "string" ? localeService.t(menuItem.tooltip) : void 0,
 			onClick: () => {
+				onMenuItemEnter(menuItem);
 				clearSubmenuCloseTimer();
 				if (hasSubmenu) {
 					if (canExecuteItem) {
@@ -4645,7 +4656,7 @@ function ContextMenuMenuItem(props) {
 				});
 			},
 			children: [contentNode, hasSubmenu && !compact && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.MoreIcon, { className: "univer-size-3.5 univer-text-gray-400 dark:!univer-text-gray-200" })]
-		}), hasSubmenu && submenuVisible && (submenuPortalContainer ? (0, react_dom.createPortal)(/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
+		}), hasSubmenu && activeSubmenuVisible && (submenuPortalContainer ? (0, react_dom.createPortal)(/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
 			ref: submenuElementRef,
 			dir: direction,
 			["data-u-context-menu-submenu"]: "true",
diff --git a/lib/es/index.js b/lib/es/index.js
index 6c1e55d052f6448caccdce3cc1d3204f010e95bd..3f23c57b208ba19726859d5b3338ad9a2a9aeefa 100644
--- a/lib/es/index.js
+++ b/lib/es/index.js
@@ -4354,6 +4354,7 @@ function ContextMenuMenu(props) {
 	const { menuSchemas, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds, onOptionSelect, maxMenuHeight } = props;
 	const localeService = useDependency(LocaleService);
 	const hiddenGroupStates = useContextGroupHiddenStates$1(menuSchemas);
+	const [activeMenuItem, setActiveMenuItem] = useState(null);
 	const visibleSchemas = useMemo(() => {
 		return menuSchemas.filter((item) => {
 			if (!hasRenderableContextMenuSchema(item)) return false;
@@ -4369,6 +4370,8 @@ function ContextMenuMenu(props) {
 			menuItem: menuSchema.item,
 			menuSessionVersion,
 			submenuPortalContainer,
+			activeMenuItem,
+			onMenuItemEnter: setActiveMenuItem,
 			onOptionSelect,
 			maxMenuHeight,
 			hiddenItemIds
@@ -4376,6 +4379,7 @@ function ContextMenuMenu(props) {
 		if (!((_menuSchema$children2 = menuSchema.children) === null || _menuSchema$children2 === void 0 ? void 0 : _menuSchema$children2.length)) return null;
 		if (menuSchema.quickLayout) return /* @__PURE__ */ jsx("div", {
 			className: clsx("univer-py-1", hasSeparator && borderBottomClassName),
+			onMouseEnter: () => setActiveMenuItem(null),
 			children: menuSchema.quickLayout === "tile" ? /* @__PURE__ */ jsx(UIQuickTileMenuGroup, {
 				item: menuSchema,
 				activeItemIds,
@@ -4395,6 +4399,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4412,6 +4418,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4421,7 +4429,7 @@ function ContextMenuMenu(props) {
 	}) });
 }
 function ContextMenuMenuItem(props) {
-	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
+	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeMenuItem, onMenuItemEnter, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
 	const localeService = useDependency(LocaleService);
 	const direction = useObservable(localeService.direction$);
 	const menuManagerService = useDependency(IMenuManagerService);
@@ -4433,6 +4441,7 @@ function ContextMenuMenuItem(props) {
 	const selectionsFromObservable = useObservable(isObservable(selectorItem.selections) ? selectorItem.selections : void 0);
 	const [inputValue, setInputValue] = useState(value);
 	const [submenuVisible, setSubmenuVisible] = useState(false);
+	const activeSubmenuVisible = submenuVisible && activeMenuItem === menuItem;
 	const [submenuPosition, setSubmenuPosition] = useState({
 		left: 0,
 		top: 0
@@ -4481,7 +4490,7 @@ function ContextMenuMenuItem(props) {
 	}, [value]);
 	useEffect(() => () => clearSubmenuCloseTimer(), [clearSubmenuCloseTimer]);
 	useEffect(() => {
-		if (!submenuVisible) {
+		if (!activeSubmenuVisible) {
 			setSubmenuPositionReady(false);
 			return;
 		}
@@ -4512,7 +4521,7 @@ function ContextMenuMenuItem(props) {
 			window.removeEventListener("scroll", updateSubmenuPosition, true);
 		};
 	}, [
-		submenuVisible,
+		activeSubmenuVisible,
 		hasSelectionSubmenu,
 		hasSubItemSubmenu
 	]);
@@ -4563,6 +4572,7 @@ function ContextMenuMenuItem(props) {
 		ref: menuItemElementRef,
 		className: "univer-relative",
 		onMouseEnter: () => {
+			onMenuItemEnter(menuItem);
 			clearSubmenuCloseTimer();
 			if (hasSubmenu && !disabled) {
 				setSubmenuPositionReady(false);
@@ -4587,6 +4597,7 @@ function ContextMenuMenuItem(props) {
 			disabled,
 			title: compact && typeof menuItem.tooltip === "string" ? localeService.t(menuItem.tooltip) : void 0,
 			onClick: () => {
+				onMenuItemEnter(menuItem);
 				clearSubmenuCloseTimer();
 				if (hasSubmenu) {
 					if (canExecuteItem) {
@@ -4615,7 +4626,7 @@ function ContextMenuMenuItem(props) {
 				});
 			},
 			children: [contentNode, hasSubmenu && !compact && /* @__PURE__ */ jsx(MoreIcon, { className: "univer-size-3.5 univer-text-gray-400 dark:!univer-text-gray-200" })]
-		}), hasSubmenu && submenuVisible && (submenuPortalContainer ? createPortal(/* @__PURE__ */ jsx("div", {
+		}), hasSubmenu && activeSubmenuVisible && (submenuPortalContainer ? createPortal(/* @__PURE__ */ jsx("div", {
 			ref: submenuElementRef,
 			dir: direction,
 			["data-u-context-menu-submenu"]: "true",
diff --git a/lib/index.js b/lib/index.js
index 6c1e55d052f6448caccdce3cc1d3204f010e95bd..3f23c57b208ba19726859d5b3338ad9a2a9aeefa 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -4354,6 +4354,7 @@ function ContextMenuMenu(props) {
 	const { menuSchemas, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds, onOptionSelect, maxMenuHeight } = props;
 	const localeService = useDependency(LocaleService);
 	const hiddenGroupStates = useContextGroupHiddenStates$1(menuSchemas);
+	const [activeMenuItem, setActiveMenuItem] = useState(null);
 	const visibleSchemas = useMemo(() => {
 		return menuSchemas.filter((item) => {
 			if (!hasRenderableContextMenuSchema(item)) return false;
@@ -4369,6 +4370,8 @@ function ContextMenuMenu(props) {
 			menuItem: menuSchema.item,
 			menuSessionVersion,
 			submenuPortalContainer,
+			activeMenuItem,
+			onMenuItemEnter: setActiveMenuItem,
 			onOptionSelect,
 			maxMenuHeight,
 			hiddenItemIds
@@ -4376,6 +4379,7 @@ function ContextMenuMenu(props) {
 		if (!((_menuSchema$children2 = menuSchema.children) === null || _menuSchema$children2 === void 0 ? void 0 : _menuSchema$children2.length)) return null;
 		if (menuSchema.quickLayout) return /* @__PURE__ */ jsx("div", {
 			className: clsx("univer-py-1", hasSeparator && borderBottomClassName),
+			onMouseEnter: () => setActiveMenuItem(null),
 			children: menuSchema.quickLayout === "tile" ? /* @__PURE__ */ jsx(UIQuickTileMenuGroup, {
 				item: menuSchema,
 				activeItemIds,
@@ -4395,6 +4399,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4412,6 +4418,8 @@ function ContextMenuMenu(props) {
 				menuItem: childSchema.item,
 				menuSessionVersion,
 				submenuPortalContainer,
+				activeMenuItem,
+				onMenuItemEnter: setActiveMenuItem,
 				activeItemIds,
 				hiddenItemIds,
 				onOptionSelect,
@@ -4421,7 +4429,7 @@ function ContextMenuMenu(props) {
 	}) });
 }
 function ContextMenuMenuItem(props) {
-	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
+	const { menuKey, menuItem, menuSessionVersion, submenuPortalContainer, activeMenuItem, onMenuItemEnter, activeItemIds, hiddenItemIds = [], compact = false, onOptionSelect, maxMenuHeight } = props;
 	const localeService = useDependency(LocaleService);
 	const direction = useObservable(localeService.direction$);
 	const menuManagerService = useDependency(IMenuManagerService);
@@ -4433,6 +4441,7 @@ function ContextMenuMenuItem(props) {
 	const selectionsFromObservable = useObservable(isObservable(selectorItem.selections) ? selectorItem.selections : void 0);
 	const [inputValue, setInputValue] = useState(value);
 	const [submenuVisible, setSubmenuVisible] = useState(false);
+	const activeSubmenuVisible = submenuVisible && activeMenuItem === menuItem;
 	const [submenuPosition, setSubmenuPosition] = useState({
 		left: 0,
 		top: 0
@@ -4481,7 +4490,7 @@ function ContextMenuMenuItem(props) {
 	}, [value]);
 	useEffect(() => () => clearSubmenuCloseTimer(), [clearSubmenuCloseTimer]);
 	useEffect(() => {
-		if (!submenuVisible) {
+		if (!activeSubmenuVisible) {
 			setSubmenuPositionReady(false);
 			return;
 		}
@@ -4512,7 +4521,7 @@ function ContextMenuMenuItem(props) {
 			window.removeEventListener("scroll", updateSubmenuPosition, true);
 		};
 	}, [
-		submenuVisible,
+		activeSubmenuVisible,
 		hasSelectionSubmenu,
 		hasSubItemSubmenu
 	]);
@@ -4563,6 +4572,7 @@ function ContextMenuMenuItem(props) {
 		ref: menuItemElementRef,
 		className: "univer-relative",
 		onMouseEnter: () => {
+			onMenuItemEnter(menuItem);
 			clearSubmenuCloseTimer();
 			if (hasSubmenu && !disabled) {
 				setSubmenuPositionReady(false);
@@ -4587,6 +4597,7 @@ function ContextMenuMenuItem(props) {
 			disabled,
 			title: compact && typeof menuItem.tooltip === "string" ? localeService.t(menuItem.tooltip) : void 0,
 			onClick: () => {
+				onMenuItemEnter(menuItem);
 				clearSubmenuCloseTimer();
 				if (hasSubmenu) {
 					if (canExecuteItem) {
@@ -4615,7 +4626,7 @@ function ContextMenuMenuItem(props) {
 				});
 			},
 			children: [contentNode, hasSubmenu && !compact && /* @__PURE__ */ jsx(MoreIcon, { className: "univer-size-3.5 univer-text-gray-400 dark:!univer-text-gray-200" })]
-		}), hasSubmenu && submenuVisible && (submenuPortalContainer ? createPortal(/* @__PURE__ */ jsx("div", {
+		}), hasSubmenu && activeSubmenuVisible && (submenuPortalContainer ? createPortal(/* @__PURE__ */ jsx("div", {
 			ref: submenuElementRef,
 			dir: direction,
 			["data-u-context-menu-submenu"]: "true",
